import { Router } from 'express';

const router = Router();

// Mount route modules here, for example:
// import usersRouter from './users';
// router.use('/users', usersRouter);

export default router;
